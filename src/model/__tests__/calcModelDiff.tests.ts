import {
    KnaveModel, Table, Column, ColumnType, Index,
} from '../model';
import {
    ModelDiff, AlteredTable, AlteredColumn,
} from '../modelDiff';

import { calcModelDiff } from '../calcModelDiff';

describe("calcModelDiff()", () => {
    it("xx", ()=> {
        expect(true).toBe(true);
    }
});